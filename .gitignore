# caches / ambiente
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

# dados gerados (snapshots + sqlite) — regeneráveis; NÃO versionar
scm_analytics/dados/*.sqlite
scm_analytics/dados/*.csv

# Obsidian: mantém config/plugins, ignora estado volátil de UI
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache
scm_analytics/dados/*.png
scm_analytics/dados/*.json

# StatsBomb open-data: clone de TERCEIROS (~18 GB, repo + licença próprios), regenerável por
# `git clone`. NUNCA versionar (estoura o GitHub e re-distribui repo alheio). Re-clone quando precisar.
scm_analytics/open-data/
open-data/

# xG DERIVADO (pequeno, auditável): por padrão segue ignorado pela regra dados/*.csv acima.
# Recomendado COMMITAR p/ reproduzir sem o clone de 18 GB — descomente a linha abaixo:
# !scm_analytics/dados/xg.csv
