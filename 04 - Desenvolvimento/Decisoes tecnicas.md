---
tags: [dev, decisoes, adr]
status: vivo
tipo: decisoes
data: 2026-06-15
---

# Decisões técnicas (ADRs)
Registro curto de *por que* cada escolha. Vivo — append quando algo for decidido.

| # | Decisão | Por quê |
|---|---|---|
| D-01 | **Contrato congelado na v5.0** ([[camada1-planejamento-v5]]) | base estável p/ o backtest; mudar fórmula = nova versão |
| D-02 | **Sem ML/boosting/bayes hierárquico** | matam auditabilidade ou garantem overfit na amostra minúscula |
| D-03 | **Custo R$ 0, roda local** | restrição de projeto; nada lê internet no cálculo (snapshot) |
| D-04 | **Probabilidades, nunca certezas** | inclusive sobre o próprio modelo (não-validado até o backtest) |
| D-05 | **Portão de backtest** ([[camada2-planejamento-v1]] §6) | nenhum termo entra "porque a literatura diz"; só com IC que não cruza zero |
| D-06 | **Baseline primeiro** ([[camada2-baseline-plano-v1]]) | medir o motor antes de adicionar graus de liberdade |
| D-07 | **Registro pré-jogo imutável** ([[Registro de previsoes]]) | sem isso, métrica de validação é autoengano |
| D-08 | **Mercado é benchmark, peso ≤0.20** | pode ecoar o Elo público; não é onisciente |
| D-09 | **Vault Obsidian in-place** | repositório vira o vault (decisão 2026-06-15) |
| D-10 | **Código em `scm_analytics/` (pacote `scm`)** | Python limpo runnable (`python -m scm.ingest`); notas e código na mesma raiz ([[Codigo (estrutura)]]). *Pasta renomeada de `codigo/` em 2026-06-16.* |
| D-11 | **Ingest idempotente por `natural_key`** (date\|home\|away\|tournament) | rodar a ingestão N vezes não duplica (`INSERT OR IGNORE`) |
| D-12 | **Pular jogos sem placar na ingestão** | fixtures futuras não entram em `matches` → sem nulos em chaves |
| D-13 | **Testes sem rede (fixtures); download só na máquina do usuário** | testes determinísticos; snapshot offline preserva "nada lê a internet no cálculo" |
| D-14 | **Versionamento git no PC do usuário; push por sessão** | o sandbox **não mantém `.git`** na pasta montada (o FS do mount corrompe o config) e não há conector de GitHub no registro. O `.git` é criado pela máquina do usuário; com **token fornecido na sessão**, o agente commita e dá push — mas a credencial **não persiste entre chats** (revogar o token após a sessão). Sem token, o usuário roda `git push`. |
| D-15 | **Bootstrap vetorizado (numpy)** | teste de escala (3000 jogos sintéticos) mostrou `harness`/`report` a ~17s/14s com bootstrap em Python puro → vetorizado p/ **~0.4s** (~40x). Sanidade: em dados **aleatórios** o modelo **não bate** o uniforme (Brier 0.68 > 0.667) — sem skill inventado. |
| D-19 | **Calor (E3) NÃO adotada** | portão over/under **+0,0007**, IC [−0,0008, +0,0022] **cruza zero** (n=15.378 quentes; κ ótimo no treino = 0,01, o menor). Causas prováveis: climatologia mensal é **proxy grosseiro** (não o WBGT do dia/horário) e o efeito é sutil/absorvido pelo estilo. Não entra como termo de λ. |
| D-20 | **Confiança = reliab(p_max)·maturidade** | A forma antiga `g_rating=1−min(0.6,σ_dr/σ_ref)` travava a confiança em ~68 — o `σ_ajuste` do jogo hipotético pisava o `σ_dr`. Nova confiança ancora na **confiabilidade medida**: curva isotônica de acerto do 'top pick' por faixa de p_max (`calibrate_confidence`, grava `meta`), × maturidade do rating (σ_R). Massacre maduro→~76 (alta), parelho→~30, provisório derruba. **Não infla nada** — a curva vem do backtest. Rótulos ≥60/40/<40. |
| D-21 | **Mercados são releituras, não novos modelos** | over/under 0.5–4.5, totais por time, clean sheet, dupla chance, handicap e **quem marca 1º** saem da MESMA matriz Poisson (`predictor.markets`); 'quem marca 1º' por Poisson concorrente `λ_i/(λ_A+λ_B)·(1−e^-(λ_A+λ_B))`. Zero graus de liberdade novos → **não passam por portão** (são exatos dado λ). 'Tempo do gol' fica fora até ingerir o minuto (`goalscorers.csv`). |
| D-22 | **Piso de λ conserva T_m (P01)** | Quando o azarão vai ao piso `λ_min`, o favorito é descontado p/ manter `λ_A+λ_B=T_m` (antes a soma furava o total → over/BTTS inflado em massacres). Bug do audit técnico. Bump p/ `baseline-v0.2.1`. |
| D-23 | **Estilo (tendência de gols) é candidato, portão na Brier de BTTS** | Feature dormente do contrato (sempre 1.0, P10 do audit). Implementada (`estilo.py`, shrinkage+cap, PIT). Como move o TOTAL e não o 1X2, o portão mede a **Brier de BTTS** (mesma lição do calor). Só entra no pipeline default se passar. É a alavanca natural do BTTS. **→ RODADO E REJEITADO** (n=445): corrige o viés médio (BTTS 50,5%→47,0% vs real 46,7%) mas ΔBrier-BTTS −0,0008 IC[−0,0083,+0,0069] **cruza zero** (sem skill por jogo; ~200 DOF por time). O viés é GLOBAL → vai p/ T_base (D-25). |
| D-24 | **Curva de confiança carimba a versão do modelo (P09)** | `calibrate_confidence` grava `{model, curve}` em `meta`; `predict_match` sinaliza `reliab_stale` se a versão não bater. Sugestões de nome agora por `difflib` (P12). |
| D-25 | **T_base calibrado na Brier de BTTS/over (não 1X2)** | O audit+dúvida do usuário expuseram BTTS ~4pp alto. O grid antigo media 1X2 (cego a T_base). `calibrate_total.py` escolhe T_base no treino minimizando viés de BTTS/over, valida no teste com **guarda de não-regressão do 1X2** (IC). 1 grau de liberdade, auditável. Candidato — rodar e adotar se passar. |
| D-18 | **Altitude (E1) ADOTADA → v0.2** | portão: ganho de Brier **+0,0491**, IC95 [+0,028, +0,070] nos **554 jogos de altitude** (θ=0,5 McSharry, sem p-hacking). Mesmo com o Elo, sobra sinal. `gd_alt` ativo no predictor; =0 fora de altitude. |
| D-17 | **Calibração C2.5 NÃO adotada (mantém v0.1)** | grid treino/teste deu ganho **+0,0013** no teste (IC [+0,0001,+0,0025]) — significativo por um fio, **irrelevante na prática**. Placeholders da v5 confirmados quase ótimos. Não se troca versão por isso. |
| D-16 | **Índice git em `/tmp` no sandbox** | o FS do mount corrompe o `.git/index` (chegou a apagar 2 arquivos de teste num commit — recuperados). Mitigação: as operações git do **agente** usam `GIT_INDEX_FILE=/tmp/...`; o `.git` do **usuário** (FS real) não tem o problema. || D-26 | **Curva de empate empírica C1 ADOTADA → v0.3** ([[Auditoria tecnica externa (2026-06-18)\|audit externo]] #3) | O `draw_prob` rodava o **proxy fechado** que a revisao-v5 (V6) proibiu. Agora `predictor.DRAW_CURVE` é a **tabela empírica P_E(\|dr\|) congelada do martj42** (n=49.423, dr pré-jogo), interpolada; o proxy vira fallback (`use_empirical_draw=False`). Corrige o tail (P(E) caía devagar demais). Brier não regrediu (all 0,5375→0,5366; major 0,5598→0,5595). |
| D-27 | **Baseline Elo público no portão** (audit #1, **Crítico**) | O harness só batia o **uniforme** (piso trivial). `backtest_harness.evaluate_vs_elo` adiciona o comparador **Elo-puro** (we + C1, sem forma/Poisson/altitude/ensemble) com ΔBrier pareado + IC. **Resultado:** o modelo **bate o Elo** com IC que não cruza zero — all +0,0028 IC[+0,0023,+0,0033]; **major +0,0037 IC[+0,0009,+0,0066]**. Edge pequeno mas real. |
| D-28 | **σ informativo** (audit #2) | `σ_R` era função só de nº de jogos (~40 fixo p/ toda seleção). Agora escala pela (in)consistência recente (`features_pit.vol_mult`, clamp [0,6–1,6], neutro com <5 jogos); e `predict_match` usa `σ_ajuste` da **dispersão de forma real** (não mais 40 fixo) + `banda_mando` no mando. `σ_dr` da porta da frente passa a **variar por confronto** (BRA×ARG 63, MEX×ALE 49, KSA×NZL 75 — era 80 fixo). Backtest σ_dr min 58→36. Brier não regrediu. |
| D-29 | **Cobertura de altitude p/ 2026 + índices** (audit #5, #11) | `altitude.CITY_ALT` ganhou **Guadalajara/Zapopan (1566 m)** — sede alta de 2026 que estava fora — e normalização de acentos (`_norm`); Monterrey (540 m) fica nível do mar. Portão re-rodado: **+0,0490 IC[+0,0283,+0,0690]** (n=566). Índices compostos `(home/away, date)` em `db.SCHEMA` aceleram o `features_pit`. |
| D-30 | **Cobertura de banda por faixa + propagação vetorizada** (audit #7 + perf) | `report.band_coverage_binned` mede cobertura **por faixa de p_v** (substitui o agregado trivial): 8/10 faixas dentro, e **flagra a superconfiança em [0,8–0,9]** (obs 0,74 fora de [0,84–0,92]). `elo_direct_read` precomputa os quantis da Normal 1× (não ~10M `inv_cdf`): mesma matemática, propagação ~8× mais rápida. |
| D-31 | **Avanço de mata-mata implementado** (contrato §3.2) | `predictor.knockout_advance`: `avanço_A = P(V)+P(E)·(0,5+ε·sinal(dr))`, ε=0,03 [a calibrar]. **Releitura** do 1X2 do ensemble (não novo modelo, como os mercados D-21) — soma 1, não altera as predições armazenadas, **sem bump de versão nem portão**. Exposto na CLI (`predict_match --mata-mata`) e na web (toggle). Simplificação: prorrogação+pênaltis juntos no termo de empate. **ε empírico** exigiria `shootouts.csv` do martj42 (ainda não ingerido) — follow-up. 96 testes. |
| D-32 | **Simulador de torneio (Camada 5) ENTREGUE** | `scm/simulate.py`: Monte Carlo da Copa 2026 → **P(campeão/final/semi/passar)** por seleção. Lê o sorteio de `dados/copa2026.json` (12 grupos × 4; **não inventar** → o usuário preenche), trava os jogos já disputados (martj42), amostra placares pela Poisson do modelo e resolve empates do mata-mata pelo `knockout_advance` (D-31). Invariantes nos testes: Σ P(campeão)=1, Σ P(passar)=32. Simplificações declaradas: chaveamento por sorteio aleatório (não a tabela posicional dos 8 terceiros), desempate de grupo pts→saldo→gols→moeda, sede neutra + bônus de anfitrião opcional. CLI `python -m scm.simulate` + página `/simulacao`. Insight, não validado por backtest. 100 testes. |
| D-33 | **Chaveamento oficial + calibrador de ε (mata-mata)** | (a) `simulate.py` passou a usar o **chaveamento OFICIAL da FIFA 2026** (R32 matches 73–88 → árvore → final 104; fonte: Wikipedia/Anexo C) no lugar do sorteio aleatório; os 8 terceiros são alocados às vagas por **elegibilidade** do Anexo C (matching com backtracking — testado nas **495** combinações). Efeito: caminhos reais (ex.: França sobe a semi p/ ~34%). (b) `calibrate_ko.py` mede o **ε empírico** dos pênaltis: junta o `shootouts.csv` (martj42) ao `dr` pré-jogo e calcula P(mais forte vence)−0,5. Roda na máquina do usuário (`--download`); ε segue 0,03 [a calibrar] até medir. 102 testes. |
| D-34 | **Forma recente aplicada no `predict_match`** ([[Auditoria tecnica completa (2026-06-19)\|audit 06-19]] N1) | A porta da frente calculava `dr = elo_A − elo_B + mando` e **descartava** o ponto de forma (só a dispersão entrava em σ), enquanto o backtest usa `dr_adj = dr_elo + (forma_home − forma_away)` (`features_pit`). Logo a previsão entregue **diferia do modelo validado**. Agora `predict_match` soma `(forma_A − forma_B)` ao `dr` — produção = backtest. **Não é termo novo** (já validado no backtest), não muda métricas do harness, não muda versão. Forma capada em ±`form_cap`. Teste novo `test_dr_includes_recent_form`. |
| D-35 | **Confiança usa σ_R ESCALADO** ([[Auditoria tecnica completa (2026-06-19)\|audit 06-19]] N3) | A confiança recebia `(a.sigma_r + b.sigma_r)/2` — o σ_R **bruto** (satura ~40 p/ seleção madura → maturidade ~0,8 fixa). O conserto de σ da v0.3 (D-28, `vol_mult`) ia só p/ `σ_dr`/banda, não p/ a confiança. Agora usa `(sr_a + sr_b)/2` (σ_R já escalado por `vol_mult`). Só afeta o score de confiança exibido (não λ, não 1X2, não as métricas gateadas). Assinatura de `confidence()` preservada (testes diretos seguem válidos). |
| D-36 | **`--estilo` rotulado EXPERIMENTAL** ([[Auditoria tecnica completa (2026-06-19)\|audit 06-19]] N4) | O flag aplicava o estilo, **rejeitado pelo portão** (D-23), sem aviso. Ajuda da CLI agora marca `[EXPERIMENTAL] … REJEITADO pelo portão`. Comportamento inalterado (continua opcional, fora do modelo padrão); só honestidade de interface. |
| D-37 | **Altitude no Monte Carlo do torneio** ([[Auditoria tecnica completa (2026-06-19)\|audit 06-19]] N2) | `simulate._sim_group` aplica `gd_alt` nos jogos de GRUPO do anfitrião adaptado quando `copa2026.json` traz `altitude_venues` (ex.: `{"Mexico":"Mexico City"}`); reusa `predictor.lambdas(..., gd_alt=...)` — termo já ADOTADO (D-18). Mata-mata fica neutro (sede varia) — simplificação declarada. [verificado] México: avanço 98,5%→99,9%, título 3,1%→4,0%; rivais de grupo caem; Σcampeão=1, Σpassa=32. Teste `test_altitude_boosts_host_in_groups`. |
| D-38 | **Registro prospectivo gerado por código** (audit N? / P-G) | `scm/registrar.py`: grava a previsão ANTES do jogo (carimba `versao_modelo`+`hash_inputs`), preenche resultado DEPOIS e mede Brier prospectivo. Respeita o registro imutável (D-07): `register` não duplica; `settle` só preenche linha em aberto; colunas de previsão nunca mudam. Usa `predict_match` (v0.3, já com forma D-34). CLI register/settle/report. Fecha o laço de validação real da Copa. Testes em `test_registrar`. |
| D-39 | **Dixon-Coles: TESTADO e REJEITADO pelo portão** (audit P-A) | `scm/dixon_coles.py` (candidato, OFF). τ(ρ) p/ placares baixos; ρ por MLE no treino = **−0,06** (bate a literatura), mas no portão (teste≥2015): BTTS **ΔBrier −0,0007 IC[−0,0008,−0,0006]** (DC PIORA) e ganho de placar exato não significativo (IC cruza 0). O viés de BTTS é de NÍVEL (T_base, D-25), não de correlação. Não entra (ρ=0). Igual a heat/estilo — fica como ferramenta de re-checagem. |
| D-40 | **Recalibração do 1X2: TESTADA e REJEITADA** (audit P-C) | `scm/calibrate_1x2.py` (candidato, OFF). Temperatura ótima no treino = **T=1,0** (identidade → nada a corrigir); isotônica por classe **piora** fora de amostra (ΔBrier −0,0021 IC<0; overfit de época). O modelo já é bem calibrado (ECE ≈ 0,025); a oscilação em [0,8–0,9] é ruído de amostra. Não entra. |
| D-41 | **Camada 3 — desfalques direcionais ENTREGUE** (audit P-F) | `scm/desfalques.py`: ATAQUE fora → corta GD (via `gd_alt`); DEFESA/GOLEIRO fora → mexe no `dr`. Tiers [a calibrar] (defesa Elo 35/15/5; ataque gols 0,25/0,12/0,04). Entrada por JSON que o usuário preenche (não inventa dados — sem fonte gratuita de escalações). Hook opcional em `predict_match(..., desfalques=...)` sem mexer no núcleo. [verificado] ataque-chave do mandante reduz P(V) e λ_a. Testes em `test_desfalques`. Magnitudes ficam candidatas ao portão quando houver base. |
| D-42 | **σ Glicko (RD) — candidato** (audit P-B) | `scm/sigma_glicko.py`: RD estilo Glicko-1 num passe cronológico (reusa os Elos pré-jogo de `match_ratings`); cresce com inatividade, cai com jogos informativos. Resolve a degenerescência do `σ_r(n)` (que satura ~40): [verificado] RD varia 51–64 nas top-14 (Argentina 64 vs México 51). **Candidato OFF.** Portão de cobertura de banda **RODADO** (`sigma_glicko.gate_band`, recorte torneios): a banda atual já **sobre-cobre** (~92% vs nominal 68%) e o Glicko a **alarga** (largura 0,134→0,184) sem ganho → **não adotar**. Achado lateral: o ajuste certo seria **encolher** σ_dr (banda larga demais), não trocar por Glicko. RD-PIT (`run_pit`) e o portão ficam prontos p/ re-checar. Testes em `test_sigma_glicko`. |
| D-43 | **Higiene: núcleo único + conftest + deps** (audit P-I/P-J) | (a) `predictor.ved_from_elo` centraliza a leitura V/E/D-do-Elo, antes COPIADA em `elo_direct_read`, `backtest_harness.elo_baseline_read` e `calibrate._elo_read` — [verificado] **idêntico** em 165 pontos de grade; backtest segue Brier 0,5366. (b) `tests/conftest.py` com fixtures `conn`/`mini_liga`. (c) `requirements.txt` com teto de major (`<3` etc.). `print` vs `logging` e `natural_key` com cidade ficam como baixa prioridade (saída de CLI é legítima; mudar a chave exigiria migração). |
| D-44 | **Esqueleto de ingestão de odds (3ª perna do ensemble)** ([[Auditoria tecnica completa (2026-06-19)\|audit]] P-H) | Tabela `odds_hist` no schema; `scm/odds.py`: odds decimais → prob **de-vigged** (`1/odd` normalizado), `store`/`market_read`, CLI `ingest/show/conv`. `predict_match(..., odds=(casa,empate,fora))` mistura o 1X2 com o mercado no peso **0,20** (contrato §3.8), renormaliza; mercados de λ seguem do modelo. **Sem odds → comportamento idêntico ao anterior.** Resolve a diversidade fictícia do ensemble (Poisson+Elo do mesmo dr) com o único sinal independente. Captura é **manual** (sem histórico gratuito — D-08); por isso é esqueleto: backtest com mercado fica para quando houver série. Testes em `test_odds`. `predict_match --odds CASA EMPATE FORA` mostra o mercado e o 1X2 já misturado. |
| D-45 | **Chaveamento mais provável (bracket)** ([[Auditoria tecnica completa (2026-06-19)\|audit §9]]) | `simulate.most_likely_bracket`: bracket DETERMINÍSTICO (grupo → mais pontos esperados; mata-mata → maior P(avançar), com a % de cada confronto), reusando o chaveamento oficial + altitude (D-37) + `knockout_advance`. CLI `python -m scm.simulate --bracket`. É a **"história"** (um caminho), complementar — não substitui — o Monte Carlo por seleção (`run()`): a chance do bracket EXATO é ínfima. [verificado] 2026: México campeão no caminho único (puxado pela altitude no grupo), enquanto o Monte Carlo dá Argentina/Espanha/França na frente e México ~4%. Teste `test_most_likely_bracket_structure` (31 partidas). Próximo: renderizar no web (`/bracket`). |
| D-46 | **Página web `/bracket` (dos 16 avos à final)** | `web.py`: rotas `/bracket` (template) e `/api/bracket` (JSON: `most_likely_bracket` + tabela do Monte Carlo de `run()`). `templates/bracket.html` desenha o chaveamento DOS 16 AVOS à final (dois lados convergindo, vencedor destacado + %) **lado a lado** com a tabela de % por seleção. A árvore (R32 73–88 → final 104) é a oficial (mesma de `simulate`). Recarregável (nº de sims). 100% local. [verificado] payload tem todos os 16 mids de 16-avos; campeão México (caminho único) vs MC Argentina 19% / Espanha 15% / França 11%. |
| D-47 | **σ_dr: encolher NÃO passou; mando (P-E) ~ok** | (a) **σ_dr scaling** testado no portão de banda (recorte torneios, treino/teste): cobertura é **não-estacionária** — sobre-cobre pré-2015 (92%) e **sub-cobre** pós-2015 (50% em s=1). Nenhuma escala única resolve (encolher melhora o treino e piora o teste recente). → **manter σ_dr** (não há conserto simples; é não-estacionariedade, não largura fixa). (b) **Mando (P-E):** H empírico (jogos não-neutros, n=36.348) ≈ **110–120 Elo**; o motor usa **100** (dentro do ruído: EQM 0,13796 vs 0,13778) → bem calibrado. O **+40** do anfitrião 2026 em sede ~neutra segue **juízo declarado** (sem precedente de co-sede p/ gatear). |
| D-48 | **BUG corrigido: altitude vazava p/ o mata-mata + sidebar web** | (a) **Bug (achado pelo usuário):** `most_likely_bracket` aplicava `gd_alt` também no **mata-mata** (via `_1x2`), inflando o anfitrião — México "campeão" (Brasil×México **71%**). Contradizia o D-37 (mata-mata neutro) e o Monte Carlo. **Corrigido:** `play()`/3º lugar usam `poisson_reads` SEM altitude; altitude só no grupo. [verificado] Brasil×México KO **71%→46%**; campeão do bracket vira **Argentina** (alinha com o MC: ARG 20% / ESP 15% / FRA 11%). O México agora cai nas oitavas (sede neutra). (b) **Web:** `templates/bracket.html` reescrito no **tema claro** do app (azul #2563eb); **sidebar** de navegação consistente (Prever confronto / Simular Copa / Chaveamento) nas 3 telas (`index`/`simulacao`/`bracket`), substituindo os links de header avulsos. |
| D-49 | **UI: bracket redesenhado + odds na previsão; altitude θ revisada por confederação** | (a) **Bracket** (`bracket.html`): abas **Chaveamento / Probabilidades** (o Monte Carlo saiu da lateral p/ uma aba), cards largos com nome em ellipsis e % fixa (corrige o "apertado"/sobreposição do screenshot do usuário). (b) **Odds na UI** (`index.html` + `web.py /api/predict` aceita `oh/od/oa`): campos de odds opcionais → mostra "Mercado (sem vig)" e mistura 20% no 1X2 (D-44). (c) **Altitude θ por confederação** (revisão pedida): `altitude.gate_by_confederation` + `--by-confed`. [verificado] θ=0,5 **sustenta nas DUAS** — CONMEBOL ΔBrier +0,066 IC[+0,035,+0,096]; CONCACAF +0,023 IC[+0,001,+0,045]; θ ótimo ≈0,5 em ambas → **mantém θ único 0,5**. Conclusão: a força do México em casa é **real**; a superestimação era só o vazamento da altitude no mata-mata (D-48), não o θ do grupo. |
| D-50 | **xG (StatsBomb) — esqueleto** | Tabela `team_xg` + `scm/xg.py` (ingest CSV `team,xg_for,xg_against,n_games` → `team_xg`; `xg_factor` = ataque/defesa encolhidos a 1.0, capados). Prior de estilo menos ruidoso que gols brutos; **candidato OFF** — não entra em λ sem portão, e o StatsBomb cobre só 2018/2022/Euro (download na máquina do usuário). Wire no `predict` + gate = futuro. |
| D-51 | **Registro prospectivo OPERACIONAL** (usar a cada rodada) | `registrar.register_batch` (registra a rodada de um JSON de fixtures, idempotente) + `settle_from_db` (preenche o resultado automaticamente a partir do snapshot martj42 re-ingerido). `register` agora **auto-carrega** desfalques (`dados/desfalques.json`, chave date\|home\|away) e odds (`odds_hist` → pseudo-odds 1/p) do confronto. Fluxo da Copa: `register-batch` antes → `ingest --download` + `settle-from-db` depois → `report` (Brier real). [verificado] México 2×0 South Africa auto-preenchido (Brier 0,097). Template `dados/desfalques.json.example`. |
| D-52 | **Explicador de previsão + desempate por confronto direto + sede por jogo** | (a) `predict_match` expõe `form_a/form_b/mando`; `index.html` mostra "como o modelo chega no dr" (Elo/forma/mando/desfalque) + o saldo (altitude/desfalque-ataque). (b) `simulate._sim_group`: empate em pts/saldo/gols resolvido pelo **confronto direto** (regra FIFA) antes do sorteio [verificado]; e `alt_venues` aceita chave por jogo `"A\|B": cidade` (sede real do calendário, além do default por anfitrião). |
| *D-53…D-74* | *(registradas nas seções "▶ Atualização" do [[CLAUDE]])* | altitude/MC, registrar, DC, σ-Glicko, σ_dr, odds, bracket, perna AD, AD-λ na sim, tempo do gol (D-71), cartões/escanteios (D-72), σ-por-time na sim (D-74)… |
| D-75 | **ε do mata-mata fechado em 0,049 [MEDIDO]** | `calibrate_ko` sobre `shootouts.csv` (n=677): P(mais forte vence nos pênaltis)=0,549 → **ε̂=+0,049 IC95 Wilson [+0,012,+0,087]**; o 0,03 antigo caía dentro do IC. `PredictParams.eps_ko 0,03→0,049` — **só-simulação, sem rebuild, sem bump** do modelo de 1 jogo. Título sub-powered (~3 disputas/Copa, gate de avanço sem potência) → adoção por **calibração direta do parâmetro**, não por portão de Brier. |
| D-76 | **`monitor`: calibração + DRIFT por mercado (power-aware)** | `monitor.reliability_by_market` (1X2 top-pick, Over 2.5, BTTS) sobre o registro imutável: previsto×observado, Brier vs taxa-base, **z-score binomial**. Com n<10 marca `n baixo` e **não alarma** (não confunde ruído com deriva — disciplina do portão). **Instrumentação, sem portão, sem versão.** `tests/test_monitor.py` (5 casos). Próximo: capturar linha de **fechamento** (`source='close'`) p/ CLV real. |
| D-77 | **Captura SEMI-AUTO da linha de fechamento (`odds_close.py`)** | Não há feed de odds **grátis/programático/confiável** da Copa (The Odds API cobra futebol; scraping frágil/ToS) → automação 100% desassistida furaria o R$0. Resolve o que falha de verdade (humano: esquecer/perder a hora): `watch` avisa (console + pop-up nativo Windows, sem deps) ~5 min antes de cada jogo sem `close`; `set` de-viga (reusa `scm.odds`) e grava `source='close'`; `due` lista pendentes. Fixtures aceitam `kickoff` ISO opcional. Captura é passo à parte (como `ingest --download`) → **não fere "nada lê a internet no cálculo"**. Launcher `.bat`; `tests/test_odds_close.py` (6 casos). Instrumentação, sem portão. Alimenta o CLV do `monitor` (D-76). |
